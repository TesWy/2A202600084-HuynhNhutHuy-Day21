# Chon mot trong: logistic_regression, random_forest, extra_trees, gradient_boosting, mlp, lnn
# CI verification run after enabling DagsHub MLflow secrets
model_type: random_forest
n_estimators: 1200
max_depth: null
min_samples_split: 2
min_samples_leaf: 1
max_features: 0.8
class_weight: balanced_subsample
n_jobs: -1
